# Data Extractor Service

Сервис для извлечения данных из файлов и поиска N-го числа.

# Инструкция по запуску сервиса

## 🚀 Варианты запуска

### 1. Локальный запуск (через IDE)

**Шаги:**
1. Запустите основное приложение (класс с аннотацией `@SpringBootApplication`)
2. Используйте Postman:

**Настройки запроса:**
- **URL:** `http://localhost:8080/extract`
- **Метод:** POST
- **Тело запроса (JSON):**

```json
{
    "path": "C:/your/path/needFile.xlsx",
    "numberPosition": 1
}
```
**Отправьте запрос.**

### 2. Запуск через Docker Compose
**Шаги:**

1. Откройте Docker Compose файл
2. Измените путь в Docker Compose
3. Используйте Postman

**В секции volumes измените путь на свой локальный путь к файлу:**
```http
volumes:
  - C:/your/path/toFile:/app/data
```  
**Замените C:/your/path/toFile  на ваш локальный путь**

**Запустите Docker Compose:**

```http
docker compose -f docker-compose-deploy.yaml up -d
```
**Настройки запроса:**
- **URL:** `http://localhost:8080/extract`
- **Метод:** POST
- **Тело запроса (JSON):**

```json
{
    "path": "/app/data/needFile.xlsx",
    "numberPosition": 1
}
```
**Отправьте запрос.**

📚 Документация API
Swagger доступен по адресу:


http://localhost:8080/swagger-ui/index.html

**Рекомендации по улучшению :**

1. Добавить бд для хранения(например Postgres).
2. Кеш для частых запросов(например Redis).
3. Инвалидация кэша и обновление  (например через @Sheduled проверять не поменялся ли исходный файл откуда брали данные для расчета
и если данные добавились или убрали существующие то запустить по новой алгоритм и сохранить в бд 
в конце инвалидипрвать кеш).
4. Добавить асинхронный подход например через CompletableFurure для быстрого ответа чтобы юзер не ждал
и добавить метод проверки результата запроса.
5. Расширить класс чтения данных из файлов других форматов(например чтение из word, txt файлов).
6. Расширить класс сортировки для использования других алгоритмов.
7. Добавить другие возможности поиска числа (например N-e максимальное число).
8. Создать свою фабрику чтобы выбрать конкретную реализацию(Фабрика четния данных из конкретных форматов, фабрика сортировок).